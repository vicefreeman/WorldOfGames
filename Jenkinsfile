pipeline {
    agent any

    environment {
        DOCKER_IMAGE = "vicefreeman/python_flask_wog"
    }

    stages {
        stage('Test') {
            steps {
                sh 'echo "SUCCESS" '
            }
        }
    }
}
